apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: flask
spec:
  ingressClassName: traefik
  rules:
  - http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: flask
            port:
              number: 80
